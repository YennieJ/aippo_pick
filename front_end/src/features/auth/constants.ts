/** AsyncStorage 키 — 로그인 응답의 `user` JSON (민감하지 않은 프로필 캐시) */
export const AUTH_USER_STORAGE_KEY = 'aippopick.authUserProfile';

/** AsyncStorage 키 — 카카오 프로필 캐시 (마이페이지 표시용) */
export const AUTH_KAKAO_STORAGE_KEY = 'aippopick.kakaoProfile';
